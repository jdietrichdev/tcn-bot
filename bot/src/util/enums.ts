export enum VoteType {
  VOUCH = "VOUCH",
  OPPOSE = "OPPOSE",
  NOT_SURE = "NOT SURE",
}

export enum ProposalType {
  PROMOTION = "Promotion",
  DEMOTION = "Demotion",
}

export enum Rank {
  ELDER = "Elder",
  LEAD = "Lead",
}
