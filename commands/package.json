{
  "name": "commands",
  "version": "1.0.0",
  "scripts": {
    "register-commands": "npx ts-node register-commands.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "1.7.2"
  }
}
